#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
 */

int main(void)
{
	int N;

	for (N = 0 ; N < 10 ; N++)
	{
		printf("%d", N);
	}
	printf("\n");

	return (0);

}
